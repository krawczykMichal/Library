apiVersion: v1
kind: Service
metadata:
  name: library-backend-service
spec:
  selector:
    app: library-backend
  ports:
    - protocol: TCP
      port: 8190
      targetPort: 8190
      nodePort: 30080
  type: NodePort